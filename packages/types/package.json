{
  "name": "@complianceos/types",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint . --ext ts --report-unused-disable-directives --max-warnings 0",
    "test": "echo 'No tests specified for types package'",
    "build": "tsc",
    "dev": "tsc -w"
  },
  "devDependencies": {
    "eslint": "^8.45.0",
    "typescript": "^5.0.2",
    "@complianceos/eslint-config": "workspace:*",
    "@complianceos/typescript-config": "workspace:*"
  }
}
